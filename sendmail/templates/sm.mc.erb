divert(-1)
divert(0)dnl
VERSIONID(`sendmail.mc (Sun)')
OSTYPE(`solaris8')dnl
DOMAIN(`generic')dnl
define(`SMART_HOST', `smtp:<%= @settings['smart_relayhost'] %>')dnl
FEATURE(`relay_entire_domain')dnl
MAILER(`smtp')dnl

LOCAL_NET_CONFIG
R$* < @ $* .$m. > $*    $#esmtp $@ $2.$m $: $1 < @ $2.$m. > $3
